// Package gateway хранит локальную конфигурацию Cosmo Router.
//
// Пакет не запускает router самостоятельно: composition выполняется через
// `task compose`, а runtime-конфигурация хранится в `config.yaml`.
package gateway
